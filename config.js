const config = {
  app: {
    port: 5000,
  },
  db: {
    host: "localhost",
    user: "root",
    password: "root",
    database: "toppantest",
    multipleStatements: true,
  },
};

export default config;
